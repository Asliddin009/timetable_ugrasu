import 'package:flutter/material.dart';
class DetailTimetableScreen extends StatelessWidget {
  const DetailTimetableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(children: [

      ],),
    );
  }
}


